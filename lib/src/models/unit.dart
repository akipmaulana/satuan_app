import 'package:flutter/material.dart';

class Unit {
  final int index;
  final String title;
  final String abbreviation;
  final double value;

  const Unit({this.index, this.title, this.abbreviation, this.value});
}